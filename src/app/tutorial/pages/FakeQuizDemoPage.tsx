"use client";

import DemoQuiz from '@/components/demo-quiz';

export default function FakeQuizDemoPage() {
    return (
        <main className="flex min-h-screen flex-col items-center justify-center p-4">
            <DemoQuiz />
        </main>
    );
}
